# Copy to .env if you want to customize the server.
PORT=3000
HOST=127.0.0.1
# Use a long password before deploying publicly; omit for a generated local password.
# MUBIEY_ADMIN_PASSWORD=replace-with-at-least-16-characters
# For public hosting: HOST=0.0.0.0 and your HTTPS origin below.
# PUBLIC_ORIGIN=https://studio.example.com
# DATA_DIR=/persistent-storage/mubiey
