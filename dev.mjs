import http from 'node:http';import fs from 'node:fs/promises';import path from 'node:path';import {fileURLToPath} from 'node:url';import handler from './api/handler.js';
const root=path.join(path.dirname(fileURLToPath(import.meta.url)),'public'),port=Number(process.env.PORT)||3011;
http.createServer(async(req,res)=>{res.status=n=>{res.statusCode=n;return res};res.json=d=>{res.setHeader('Content-Type','application/json');res.end(JSON.stringify(d));return res};const p=new URL(req.url,'http://localhost').pathname;if(p.startsWith('/api/')||p.startsWith('/media/'))return handler(req,res);if(!['GET','HEAD'].includes(req.method)){res.statusCode=405;return res.end()}const file=path.resolve(root,p.startsWith('/invite/')?'guest.html':p==='/'?'index.html':p.slice(1));if(!file.startsWith(root+path.sep)){res.statusCode=403;return res.end()}try{const content=await fs.readFile(file);res.setHeader('Content-Type',({'.html':'text/html','.css':'text/css','.js':'text/javascript','.svg':'image/svg+xml'})[path.extname(file)]||'application/octet-stream');res.end(req.method==='HEAD'?undefined:content)}catch{res.statusCode=404;res.end('Not found')}}).listen(port,'127.0.0.1',()=>console.log('Mubiey Studio: http://localhost:'+port));
