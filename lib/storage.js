import {createClient} from '@supabase/supabase-js';
let client;
export function storage(){const {SUPABASE_URL,SUPABASE_SERVICE_ROLE_KEY}=process.env;if(!SUPABASE_URL||!SUPABASE_SERVICE_ROLE_KEY)throw Error('Missing Supabase settings');client??=createClient(SUPABASE_URL,SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}});return client.storage.from('mubiey-private')}
export function bucket(){const b=storage();return{async delete(key){const {error}=await b.remove([key]);if(error)throw error},async signed(key,download){const {data,error}=await b.createSignedUrl(key,60,download?{download}:{});if(error)throw error;return data.signedUrl},async signUpload(key){const {data,error}=await b.createSignedUploadUrl(key,{upsert:false});if(error)throw error;return data.signedUrl},async info(key){const {data,error}=await b.info(key);if(error)throw error;return data}}}
